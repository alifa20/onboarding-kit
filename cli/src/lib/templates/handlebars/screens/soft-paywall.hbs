/**
 * Soft Paywall Screen for {{projectName}}
 * Generated by OnboardKit
 */

import React from 'react';
import { View, Text, StyleSheet, SafeAreaView, ScrollView } from 'react-native';
import { useNavigation } from '@react-navigation/native';
import type { NativeStackNavigationProp } from '@react-navigation/native-stack';
import { Button } from '../components/Button';
import { Card } from '../components/Card';
import { colors, typography, spacing } from '../theme';
import type { RootStackParamList } from '../navigation/types';

type NavigationProp = NativeStackNavigationProp<RootStackParamList, 'SoftPaywall'>;

export function SoftPaywallScreen() {
  const navigation = useNavigation<NavigationProp>();

  const handleStartTrial = () => {
    // TODO: Implement subscription flow
    navigation.navigate('Login');
  };

  const handleSkip = () => {
    navigation.navigate('Login');
  };

  return (
    <SafeAreaView style={styles.container}>
      <ScrollView contentContainerStyle={styles.scrollContent}>
        <View style={styles.content}>
          <View style={styles.header}>
            <Text style={styles.headline}>{{softPaywall.headline}}</Text>
            <Text style={styles.subtext}>{{softPaywall.subtext}}</Text>
          </View>

          <View style={styles.pricing}>
            <Text style={styles.price}>{{softPaywall.price}}</Text>
          </View>

          <View style={styles.features}>
            {{#each softPaywall.features}}
            <View style={styles.featureItem}>
              <Text style={styles.featureIcon}>✓</Text>
              <Text style={styles.featureText}>{{this}}</Text>
            </View>
            {{/each}}
          </View>

          <View style={styles.buttonContainer}>
            <Button
              title="{{softPaywall.cta}}"
              variant="primary"
              fullWidth
              onPress={handleStartTrial}
            />
            {{#if softPaywall.skip}}
            <Button
              title="{{softPaywall.skip}}"
              variant="ghost"
              fullWidth
              onPress={handleSkip}
            />
            {{/if}}
          </View>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colors.background,
  },
  scrollContent: {
    flexGrow: 1,
  },
  content: {
    flex: 1,
    paddingHorizontal: spacing.lg,
    paddingVertical: spacing.xl,
    justifyContent: 'space-between',
  },
  header: {
    alignItems: 'center',
    marginBottom: spacing.xl,
  },
  headline: {
    ...typography.h1,
    color: colors.text,
    textAlign: 'center',
    marginBottom: spacing.md,
  },
  subtext: {
    ...typography.body,
    color: colors.textSecondary,
    textAlign: 'center',
  },
  pricing: {
    alignItems: 'center',
    marginBottom: spacing.xl,
  },
  price: {
    ...typography.h2,
    color: colors.primary,
    fontWeight: 'bold',
  },
  features: {
    marginBottom: spacing.xl,
  },
  featureItem: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: spacing.md,
  },
  featureIcon: {
    fontSize: 20,
    color: colors.success,
    marginRight: spacing.sm,
    fontWeight: 'bold',
  },
  featureText: {
    ...typography.body,
    color: colors.text,
    flex: 1,
  },
  buttonContainer: {
    gap: spacing.md,
  },
});
