/**
 * Spacing scale for {{projectName}}
 * Generated by OnboardKit
 */

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
  xxxl: 64,
} as const;

export const borderRadius = {
  sm: {{theme.borderRadius}} * 0.5,
  md: {{theme.borderRadius}},
  lg: {{theme.borderRadius}} * 1.5,
  full: 9999,
} as const;

export type SpacingSize = keyof typeof spacing;
export type BorderRadiusSize = keyof typeof borderRadius;
