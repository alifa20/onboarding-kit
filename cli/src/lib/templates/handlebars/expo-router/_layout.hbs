/**
 * Root Layout for {{projectName}}
 * Generated by OnboardKit - Expo Router
 */

import { Stack } from 'expo-router';
import React from 'react';

const screenOptions = {
  headerShown: false,
};

export default function RootLayout() {
  return (
    <Stack screenOptions={screenOptions}>
      <Stack.Screen name="index" />
      {{#each onboardingSteps}}
      <Stack.Screen name="onboarding-step-{{@index}}" />
      {{/each}}
      {{#if hasSoftPaywall}}
      <Stack.Screen name="soft-paywall" />
      {{/if}}
      <Stack.Screen name="login" />
      <Stack.Screen name="name-capture" />
      {{#if hasHardPaywall}}
      <Stack.Screen name="hard-paywall" />
      {{/if}}
      <Stack.Screen name="home" />
    </Stack>
  );
}
